query Whoami {
  whoami {
    id
    username
  }
}

mutation Signin($data: UserInput!) {
  signin(data: $data) {
    id
    username
  }
}

mutation Signout {
  signout
}

query Wallet {
  wallet {
    amount
  }
}
